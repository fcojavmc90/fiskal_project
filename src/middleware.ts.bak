import { fetchAuthSession } from 'aws-amplify/auth/server';
import { NextRequest, NextResponse } from 'next/server';

export async function middleware(request: NextRequest) {
  const response = NextResponse.next();
  
  // Aquí puedes añadir lógica para verificar grupos de Cognito
  // Por ahora, protegemos las rutas base
  if (request.nextUrl.pathname.startsWith('/dashboard-pro')) {
     // Lógica de redirección si no es PRO
  }
  
  return response;
}

export const config = {
  matcher: ['/dashboard-pro/:path*', '/dashboard-cliente/:path*'],
};
